/// <reference types="node" />
import { Eth, Tx } from '../eth';
export interface SignedTx {
    messageHash: string;
    v: string;
    r: string;
    s: string;
    rawTransaction: string;
    chainId?: any;
    gasPrice?: any;
    nonce?: number;
}
export declare function signTransaction(tx: Tx, privateKey: Buffer, eth: Eth): Promise<SignedTx>;
export declare function recoverTransaction(rawTx: string): string;
