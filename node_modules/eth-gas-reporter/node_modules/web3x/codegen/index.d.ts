#!/usr/bin/env node
import { ContractAbi } from '../contract';
export declare function makeAndWriteFiles(outputPath: string, name: string, abiLocation: string | ContractAbi, web3xPath: string): Promise<void>;
export declare function getWeb3xPath(): any;
