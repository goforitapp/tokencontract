/// <reference types="node" />
import { EventEmitter } from 'events';
import { EthereumProvider } from '../providers/ethereum-provider';
export declare class Subscription<Result = any> extends EventEmitter {
    readonly type: 'eth' | 'shh';
    readonly subscription: string;
    readonly params: any[];
    private provider;
    private id?;
    private listener?;
    constructor(type: 'eth' | 'shh', subscription: string, params: any[], provider: EthereumProvider);
    on(event: 'rawdata' | 'data' | 'changed' | 'error', listener: (result: Result) => void): this;
    once(event: 'rawdata' | 'data' | 'changed' | 'error', listener: (result: Result) => void): this;
    subscribe(): Promise<this>;
    private notificationHandler;
    unsubscribe(): void;
}
