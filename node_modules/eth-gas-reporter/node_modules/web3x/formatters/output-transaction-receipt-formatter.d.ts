import { Log } from './output-log-formatter';
export interface TransactionReceipt<Events = void> {
    transactionHash: string;
    transactionIndex: number;
    blockHash: string;
    blockNumber: number;
    from: string;
    to: string;
    contractAddress: string;
    cumulativeGasUsed: number;
    gasUsed: number;
    logs?: Log[];
    events?: Events extends void ? {
        [eventName: string]: EventLog<any>[];
    } : Events;
    unnamedEvents?: EventLog<any>[];
    status: string;
}
export interface EventLog<ReturnValues, Name = string> {
    id: string | null;
    removed?: boolean;
    event?: Name;
    address: string;
    returnValues: ReturnValues;
    logIndex: number | null;
    transactionIndex: number | null;
    transactionHash: string | null;
    blockHash: string | null;
    blockNumber: number | null;
    raw: {
        data: string;
        topics: string[];
    };
    signature: string | null;
}
/**
 * Formats the output of a transaction receipt to its proper values
 *
 * @method outputTransactionReceiptFormatter
 * @param {Object} receipt
 * @returns {Object}
 */
export declare function outputTransactionReceiptFormatter(receipt: any): any;
